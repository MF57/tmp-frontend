'use strict';

describe('myApp.enrollMe module', function() {

  beforeEach(module('myApp.enrollMe'));

  describe('tournamentCreator controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view3Ctrl = $controller('EnrollMeCtrl');
      expect(view3Ctrl).toBeDefined();
    }));

  });
});